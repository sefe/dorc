// tslint:disable
/**
 * Build
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.1-preview
 * Contact: nugetvss@microsoft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { Observable } from 'rxjs';
import type { AjaxResponse } from 'rxjs/ajax';
import type { HttpHeaders, HttpQuery, OperationOpts } from '../runtime';
import { BaseAPI, encodeURI, throwIfNullOrUndefined } from '../runtime';
import type { BuildSettings } from '../models';

export interface SettingsGetRequest {
  organization: string;
  project: string;
  apiVersion: string;
}

export interface SettingsUpdateRequest {
  organization: string;
  project: string;
  apiVersion: string;
  body: BuildSettings;
}

/**
 * no description
 */
export class SettingsApi extends BaseAPI {
  /**
   * Gets the build settings.
   */
  settingsGet({
    organization,
    project,
    apiVersion
  }: SettingsGetRequest): Observable<BuildSettings>;
  settingsGet(
    { organization, project, apiVersion }: SettingsGetRequest,
    opts?: OperationOpts
  ): Observable<AjaxResponse<BuildSettings>>;
  settingsGet(
    { organization, project, apiVersion }: SettingsGetRequest,
    opts?: OperationOpts
  ): Observable<BuildSettings | AjaxResponse<BuildSettings>> {
    throwIfNullOrUndefined(organization, 'organization', 'settingsGet');
    throwIfNullOrUndefined(project, 'project', 'settingsGet');
    throwIfNullOrUndefined(apiVersion, 'apiVersion', 'settingsGet');

    const headers: HttpHeaders = {
      ...(this.configuration.username != null &&
      this.configuration.password != null
        ? {
            Authorization: `Basic ${btoa(
              this.configuration.username + ':' + this.configuration.password
            )}`
          }
        : undefined)
    };

    const query: HttpQuery = {
      // required parameters are used directly since they are already checked by throwIfNullOrUndefined
      'api-version': apiVersion
    };

    return this.request<BuildSettings>(
      {
        url: '/{organization}/{project}/_apis/build/settings'
          .replace('{organization}', encodeURI(organization))
          .replace('{project}', encodeURI(project)),
        method: 'GET',
        headers,
        query
      },
      opts?.responseOpts
    );
  }

  /**
   * Updates the build settings.
   */
  settingsUpdate({
    organization,
    project,
    apiVersion,
    body
  }: SettingsUpdateRequest): Observable<BuildSettings>;
  settingsUpdate(
    { organization, project, apiVersion, body }: SettingsUpdateRequest,
    opts?: OperationOpts
  ): Observable<AjaxResponse<BuildSettings>>;
  settingsUpdate(
    { organization, project, apiVersion, body }: SettingsUpdateRequest,
    opts?: OperationOpts
  ): Observable<BuildSettings | AjaxResponse<BuildSettings>> {
    throwIfNullOrUndefined(organization, 'organization', 'settingsUpdate');
    throwIfNullOrUndefined(project, 'project', 'settingsUpdate');
    throwIfNullOrUndefined(apiVersion, 'apiVersion', 'settingsUpdate');
    throwIfNullOrUndefined(body, 'body', 'settingsUpdate');

    const headers: HttpHeaders = {
      'Content-Type': 'application/json',
      ...(this.configuration.username != null &&
      this.configuration.password != null
        ? {
            Authorization: `Basic ${btoa(
              this.configuration.username + ':' + this.configuration.password
            )}`
          }
        : undefined)
    };

    const query: HttpQuery = {
      // required parameters are used directly since they are already checked by throwIfNullOrUndefined
      'api-version': apiVersion
    };

    return this.request<BuildSettings>(
      {
        url: '/{organization}/{project}/_apis/build/settings'
          .replace('{organization}', encodeURI(organization))
          .replace('{project}', encodeURI(project)),
        method: 'PATCH',
        headers,
        query,
        body: body
      },
      opts?.responseOpts
    );
  }
}
