// tslint:disable
/**
 * Build
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 6.1-preview
 * Contact: nugetvss@microsoft.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { Observable } from 'rxjs';
import type { AjaxResponse } from 'rxjs/ajax';
import type { HttpHeaders, HttpQuery, OperationOpts } from '../runtime';
import { BaseAPI, encodeURI, throwIfNullOrUndefined } from '../runtime';
import type {
  ProjectRetentionSetting,
  UpdateProjectRetentionSettingModel
} from '../models';

export interface RetentionGetRequest {
  organization: string;
  project: string;
  apiVersion: string;
}

export interface RetentionUpdateRequest {
  organization: string;
  project: string;
  apiVersion: string;
  body: UpdateProjectRetentionSettingModel;
}

/**
 * no description
 */
export class RetentionApi extends BaseAPI {
  /**
   * Gets the project\'s retention settings.
   */
  retentionGet({
    organization,
    project,
    apiVersion
  }: RetentionGetRequest): Observable<ProjectRetentionSetting>;
  retentionGet(
    { organization, project, apiVersion }: RetentionGetRequest,
    opts?: OperationOpts
  ): Observable<AjaxResponse<ProjectRetentionSetting>>;
  retentionGet(
    { organization, project, apiVersion }: RetentionGetRequest,
    opts?: OperationOpts
  ): Observable<
    ProjectRetentionSetting | AjaxResponse<ProjectRetentionSetting>
  > {
    throwIfNullOrUndefined(organization, 'organization', 'retentionGet');
    throwIfNullOrUndefined(project, 'project', 'retentionGet');
    throwIfNullOrUndefined(apiVersion, 'apiVersion', 'retentionGet');

    const headers: HttpHeaders = {
      // oauth required
      ...(this.configuration.accessToken != null
        ? {
            Authorization:
              typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken('oauth2', ['vso.build'])
                : this.configuration.accessToken
          }
        : undefined)
    };

    const query: HttpQuery = {
      // required parameters are used directly since they are already checked by throwIfNullOrUndefined
      'api-version': apiVersion
    };

    return this.request<ProjectRetentionSetting>(
      {
        url: '/{organization}/{project}/_apis/build/retention'
          .replace('{organization}', encodeURI(organization))
          .replace('{project}', encodeURI(project)),
        method: 'GET',
        headers,
        query
      },
      opts?.responseOpts
    );
  }

  /**
   * Updates the project\'s retention settings.
   */
  retentionUpdate({
    organization,
    project,
    apiVersion,
    body
  }: RetentionUpdateRequest): Observable<ProjectRetentionSetting>;
  retentionUpdate(
    { organization, project, apiVersion, body }: RetentionUpdateRequest,
    opts?: OperationOpts
  ): Observable<AjaxResponse<ProjectRetentionSetting>>;
  retentionUpdate(
    { organization, project, apiVersion, body }: RetentionUpdateRequest,
    opts?: OperationOpts
  ): Observable<
    ProjectRetentionSetting | AjaxResponse<ProjectRetentionSetting>
  > {
    throwIfNullOrUndefined(organization, 'organization', 'retentionUpdate');
    throwIfNullOrUndefined(project, 'project', 'retentionUpdate');
    throwIfNullOrUndefined(apiVersion, 'apiVersion', 'retentionUpdate');
    throwIfNullOrUndefined(body, 'body', 'retentionUpdate');

    const headers: HttpHeaders = {
      'Content-Type': 'application/json',
      // oauth required
      ...(this.configuration.accessToken != null
        ? {
            Authorization:
              typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken('oauth2', [
                    'vso.build_execute'
                  ])
                : this.configuration.accessToken
          }
        : undefined)
    };

    const query: HttpQuery = {
      // required parameters are used directly since they are already checked by throwIfNullOrUndefined
      'api-version': apiVersion
    };

    return this.request<ProjectRetentionSetting>(
      {
        url: '/{organization}/{project}/_apis/build/retention'
          .replace('{organization}', encodeURI(organization))
          .replace('{project}', encodeURI(project)),
        method: 'PATCH',
        headers,
        query,
        body: body
      },
      opts?.responseOpts
    );
  }
}
